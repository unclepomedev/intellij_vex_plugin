= usd_pointinstance_getbbox_size =

#type: vex
#context: all
#tags: usd, usd_pointinstance
#group: usd
#since: 18.0

"""Returns the size of the bounding box for the instance inside a point instancer primitive."""

:usage: `vector usd_pointinstance_getbbox_size( <stage>, string primpath, int instance_index, string purpose )`

Computes the largest position of the bounding box for the instance geometry.

:include _common#usd_stage:

:include _common#usd_primpath:

:arg:instance_index:
    The index of the instance whose bounding box to return.

:arg:purpose:
    The primitive's purpose for which to return the bounding box size (e.g., "render").

:returns:
    The size of the instance's bounding box.

@examples 
{{{
#!vex
// Get the size of the first instance's boundsng box.
vector size = usd_pointinstance_getbbox_size(0, "/src/instanced_spheres", 0, "render");
}}}

@related
- [Vex:usd_getpointinstancebounds]
