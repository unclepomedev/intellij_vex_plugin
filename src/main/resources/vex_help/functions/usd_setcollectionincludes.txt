= usd_setcollectionincludes =

#type: vex
#context: all
#tags: usd, usd_collection
#group: usd
#since: 18.0

"""Sets the includes list on the collection"""

:usage: `int usd_setcollectionincludes(int stagehandle, string collectionpath, string includes[])`

This function sets the includes list on the collection.

:include _common#usd_stagehandle:

:include _common#usd_collectionpath:

:arg:includes:
    A list of object paths to set as an includes list on the collection.

:returns:
    The value of `stagehandle` on success or `-1` on failure.

@examples 
{{{
#!vex
// Set the exludes list on the cube's collection.
string collection_path = usd_makecollectionpath(0, "/geo/cube", "some_collection");
usd_setcollectionincludes(0, collection_path, array("/geo/sphere1", "/geo/sphere2"));
}}}

@related
- [Vex:usd_iscollection]
- [Vex:usd_setcollectionexcludes]
