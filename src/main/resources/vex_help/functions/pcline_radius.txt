= pcline_radius =

#type: vex
#context: all
#tags: ptcloud, proximity
#group: ptcloud
#since: 18.0

"""Returns a list of closest points to an infinite line from a specified file"""

:usage: `int[] pcline_radius(<geometry>, string PChannel, string RadChannel, float radscale, vector P, vector dir, float max_distance, int maxpoints)`
:usage: `int[] pcline_radius(<geometry>, string ptgroup, string PChannel, string RadChannel, float radscale, vector P, vector dir, float max_distance, int maxpoints)`

:include _common#geometry:

These functions open a geometry file and return a list of points (treated as spheres) within <<max_distance>> of the line passing through <<P>> with direction <<dir>>. Each point is treated as a sphere with radius equal to its <<RadChannel>> attribute, 
    multiplied by <<radscale>>.

The `ptgroup` is a point group that limits the points to search.  This is a SOP-style group pattern, so can be something like `0-10` or `@Cd.x>0.5`.  A blank string is treated as matching all points.

@related

- [Vex:nearpoint]
- [Vex:nearpoints]
- [Vex:pccone_radius]
- [Vex:pcfind]
- [Vex:pcfind_radius]
- [Vex:pgfind]
