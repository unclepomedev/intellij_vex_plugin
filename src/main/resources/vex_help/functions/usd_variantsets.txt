= usd_variantsets =

#type: vex
#context: all
#tags: usd, usd_prim, usd_variant
#group: usd
#since: 17.5

"""Returns the variant sets available on a primitive."""

:usage: `string[] usd_variantsets(<stage>, string primpath)`

This function returns the variant sets for the given primitive.

:include _common#usd_stage:

:include _common#usd_primpath:

:returns:
    The variant set names available on a given primitive.

@examples 
{{{
#!vex
// Get the variant sets available on the "shape_shifter" primitive.
string variant_sets[] = usd_variantsets(0, "/geo/shape_shifter");
}}}

@related
- [Vex:usd_isprim]
- [Vex:usd_variants]
