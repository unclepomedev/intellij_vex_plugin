= usd_setdrawmode =

#type: vex
#context: all
#tags: usd, usd_prim
#group: usd
#since: 17.5

"""Sets the primitive's draw mode."""

:usage: `int usd_setdrawmode(int stagehandle, string primpath, string mode)`

This function sets the primitive's draw mode.

:include _common#usd_stagehandle:

:include _common#usd_primpath:

:arg:mode:
    The draw mode to set the primitive to.

:returns:
    The value of `stagehandle` on success or `-1` on failure.

@examples 
{{{
#!vex
// Set the sphere to draw as bounds and the cube to draw as default.
usd_setdrawmode(0, "/geo/sphere", "bounds");
usd_setdrawmode(0, "/geo/cube", "default");
}}}

@related
- [Vex:usd_drawmode]
