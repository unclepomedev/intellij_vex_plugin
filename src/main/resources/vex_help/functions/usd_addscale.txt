= usd_addscale =

#type: vex
#context: all
#tags: usd, usd_transform
#group: usd
#since: 18.0

"""Applies a scale to the primitive""" 

:usage: `int usd_addscale(int stagehandle; string primpath; string suffix; vector scale)`

This function applies a scale to the primitive. It creates and sets a value of a transform operation attribute that defines the scale, and appends it to the primitive's transform order.

:include _common#usd_stagehandle:

:include _common#usd_primpath:

:include _common#usd_xformopsuffix:

:arg:scale:
    The the scale factors along each of the principle axes.

:returns:
    The value of `stagehandle` on success or `-1` on failure.

@examples 
{{{
#!vex
// Scale the cube
usd_addscale(0, "/geo/cube", "my_scale", {0.25, 0.5, 2});
}}}

@related
- [Vex:usd_transformorder]

