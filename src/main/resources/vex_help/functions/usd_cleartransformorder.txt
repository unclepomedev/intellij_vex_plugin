= usd_cleartransformorder =

#type: vex
#context: all
#tags: usd, usd_transform
#group: usd
#since: 17.5

"""Clears the primitive's transform order"""

:usage: `int usd_cleartransformorder(int stagehandle, string primpath)`

This function clears the primitive's transform order.  Transform order is a sequence of transform operations, whose full names are stored in `xformOpOrder` attribute as a string array.  Thus, this function clears that attribute.

:include _common#usd_stagehandle:

:include _common#usd_primpath:

:returns:
    The value of `stagehandle` on success or `-1` on failure.

@examples 
{{{
#!vex
usd_cleartransformorder(0, "/geo/cone");
}}}

@related
- [Vex:usd_transformorder]
- [Vex:usd_addinversetotransformorder]
