= usd_primvarlen =

#type: vex
#context: all
#tags: usd, usd_primvar
#group: usd
#since: 18.0

"""Returns the length of the array primvar directly on the USD primitive."""

:usage: `int usd_primvarlen(<stage>; string primpath; string name)`
:usage: `int usd_primvarlen(<stage>; string primpath; string name; float timecode)`

This function returns the length of a given primvar found directly on the given primitive. 

For array primvars it is the length of an array, and for non-array primvars the length is 1.

:include _common#usd_stage:

:include _common#usd_primpath:

:arg:name:
    Primvar name (without namespace).

:include _common#usd_timecode:

:returns:
    The length of the array primvar, or `1` if the primvar is not an array, or `0` if the primvar does not exist. Use [Vex:usd_isarrayprimvar] to check if the primvar is an array.

@examples 
{{{
#!vex
// Get the array length of the primvar on cube.
int array_length = usd_primvarlen(0, "/geo/cube", "array_primvar_name");
}}}

@related
- [Vex:usd_iprimvarlen] 
- [Vex:usd_isprimvar]
- [Vex:usd_primvarsize]
