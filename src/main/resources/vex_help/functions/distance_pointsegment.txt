= distance_pointsegment =

#type: vex
#context: all
#tags: proximity
#group: math

"""This function returns the closest distance between the point Q and a
finite line segment between points P0 and P1."""

:usage: `float distance_pointsegment(vector Q, vector P0, vector P1)`

This function returns the closest distance between the point <<Q>> and a
finite line segment between points <<P0>> and <<P1>>.
